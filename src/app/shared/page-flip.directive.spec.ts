import { PageFlipDirective } from './page-flip.directive';

describe('PageFlipDirective', () => {
  it('should create an instance', () => {
    const directive = new PageFlipDirective();
    expect(directive).toBeTruthy();
  });
});
